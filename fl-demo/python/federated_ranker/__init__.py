"""Python/PyTorch mirror of the browser demo's federated learning core.

Same algorithm as fl-demo/fl.js — DP-FedAvg with per-client update clipping
and Gaussian noise — same synthetic world as data.js, same simplified
privacy accounting. The browser demo exists to be clicked through live; this
package exists to be read, run from a terminal, and shown as real code.
"""
